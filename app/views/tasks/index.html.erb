<h1>My Tasks</h1>

<% @tasks.each do |task| %>
<% done = "--" %>
<% done = "X" if task.completed %>
<p>[<%= done %>]
<%= link_to task.title, task_path(task) %></p>
<% end  %>
<p>
<p>
<%= link_to "Create new Task", tasks_new_path %>
